# 学習ロードマップ

## この学習の目的

目的は、アセンブリを自在に書けるようになることではありません。普段書いているコードが、コンパイラによってどのような命令へ変換され、CPU、メモリ、OSを使って実行されるのかを理解することです。

```text
高級言語 → コンパイラ → アセンブリ → 機械語 → CPU／メモリ → OS
```

実験では一度に扱う疑問を小さくし、Cコード、生成されたアセンブリ、実行時の状態を対応させながら観察します。

## 観察環境

最初は手元のマシンのCPUアーキテクチャとABIに限定します。異なるアーキテクチャとの比較は、基本的な流れを理解してから行います。

各実験では、原則として次の流れを繰り返します。

1. 確かめたいことと予想を書く
2. 最小限のCコードを書く
3. 最適化なし（`-O0`）でコンパイルする
4. アセンブリと実行ファイルを観察する
5. デバッガで命令、レジスタ、メモリの変化を追う
6. 最適化あり（`-O2`）と比較する
7. 分かったことと残った疑問を記録する

## Phase 0: 観察の準備

### 理解したいこと

- 使用しているCPUアーキテクチャとOS
- コンパイラ、アセンブラ、リンカ、デバッガの役割
- ソースコードから実行ファイルができるまでの全体像
- 2進数、16進数、バイト、アドレスの最低限の読み方

### 実験

- コンパイラとデバッガのバージョンを確認する
- 空に近いCプログラムをビルドして実行する
- 前処理、コンパイル、アセンブル、リンクを分けて実行する
- 実行ファイルの形式とセクションを確認する

### 到達目標

ソースコードから実行ファイルまでの各段階と、使用する観察ツールを説明できる。

## Phase 1: CPUとレジスタ

### 理解したいこと

- CPUが命令を順番に実行する仕組み
- レジスタが計算途中の値を保持する役割
- 命令ポインタとフラグ
- 即値、レジスタ、メモリの違い

### 実験

- 定数を返す関数
- 2つの整数を加算する関数
- 複数の算術演算を含む式
- 1命令ずつ実行し、レジスタの変化を記録する

### 到達目標

Cの単純な式とCPU命令を対応させ、値がどのレジスタを通るか追跡できる。

## Phase 2: メモリ、変数、ポインタ

### 理解したいこと

- アドレスとメモリ上の値の違い
- ローカル変数が置かれる場所
- ポインタによる間接参照
- 配列の連続配置とインデックス計算
- バイト順序とデータサイズ

### 実験

- ローカル変数の読み書き
- ポインタ経由で値を変更する
- 配列の各要素のアドレスを比較する
- 異なる整数型のサイズとメモリ表現を確認する

### 到達目標

変数の値とアドレスを区別し、ポインタや配列へのアクセスがどのようなメモリ操作になるか説明できる。

## Phase 3: 分岐とループ

### 理解したいこと

- 比較命令と条件フラグ
- 条件分岐と無条件ジャンプ
- `if`、`switch`、`for`、`while`の実体
- 分岐の組み合わせで制御構造が作られること

### 実験

- `if`／`else`を含む関数
- 複数条件の短絡評価
- `for`と`while`の比較
- 小さな`switch`と大きな`switch`の比較

### 到達目標

アセンブリの比較とジャンプから、元の制御の流れを追跡できる。

## Phase 4: 関数呼び出しとスタック

### 理解したいこと

- ABIと呼び出し規約
- 引数と戻り値の受け渡し
- 戻りアドレス
- スタックポインタとスタックフレーム
- 呼び出し側と呼び出される側の役割分担

### 実験

- 引数が異なる個数の関数
- レジスタだけでは渡せない数の引数を持つ関数
- 関数の呼び出し前後でスタックを観察する
- 再帰呼び出しでフレームが積み重なる様子を追う

### 到達目標

関数を呼び出して戻るまでのレジスタとスタックの変化を図示できる。

## Phase 5: データ構造とメモリ領域

### 理解したいこと

- 構造体の配置、アラインメント、パディング
- 静的領域、スタック、ヒープの違い
- 文字列リテラルとグローバル変数の配置
- `malloc`と`free`が担う範囲

### 実験

- 構造体のフィールド順を変えてサイズを比較する
- グローバル、静的、ローカル変数のアドレスを比較する
- 動的に確保した領域をデバッガで観察する
- スタック上とヒープ上に同じ構造体を作る

### 到達目標

プログラム内のデータがどの領域に置かれ、いつまで存在するか説明できる。

## Phase 6: コンパイラ最適化

### 理解したいこと

- Cコードと機械語が一対一に対応しない理由
- 定数畳み込み、不要コード除去、インライン化
- ループ最適化とベクトル化
- 未定義動作が最適化に与える影響

### 実験

- 同じコードを`-O0`、`-O1`、`-O2`、`-O3`で比較する
- 呼び出される関数がインライン化で消える様子を見る
- 計算や分岐がコンパイル時に消える例を作る
- `volatile`の有無による違いを確認する

### 到達目標

最適化後の命令列が元のCコードと異なっていても、その理由を仮説として説明できる。

## Phase 7: 実行ファイルとプログラムの開始

### 理解したいこと

- オブジェクトファイルと実行ファイル
- シンボル、セクション、再配置
- 静的リンクと動的リンク
- `main`より前後に行われる処理
- OSが実行ファイルをプロセスとして開始する流れ

### 実験

- オブジェクトファイルのシンボルとセクションを調べる
- 複数ファイルのコンパイルとリンクを分離する
- 未解決シンボルによるリンクエラーを観察する
- プログラムのエントリーポイントから`main`までを追う

### 到達目標

機械語を含むファイルが、どのようにメモリへ配置され実行を開始するか大まかに説明できる。

## Phase 8: OSとの境界

### 理解したいこと

- ユーザーモードとカーネルモード
- 標準ライブラリとシステムコールの違い
- ファイル、標準入出力、プロセスの抽象化
- 仮想メモリとページ

### 実験

- `write`相当の処理をライブラリと直接のシステムコールで比較する
- ファイルを開いて読み書きする処理を追う
- システムコールをトレースする
- 不正なメモリアクセスがOSに検出される様子を見る

### 到達目標

CPUが通常の命令を実行する領域と、OSへ処理を依頼する境界を説明できる。

## Phase 9: 一本につなげる

最後に、小さなプログラムを題材に次の流れを通して観察します。

1. Cソースを読む
2. コンパイラが生成した命令を確認する
3. 実行ファイル内でコードとデータの位置を確認する
4. プログラムの開始から関数呼び出しを追う
5. レジスタ、スタック、ヒープの変化を追う
6. システムコールを経由して結果が外部へ出るまでを見る

### 最終到達目標

小さなCプログラムについて、ソースコードが機械語になり、OSに読み込まれ、CPUとメモリを使って実行され、結果を外部へ出すまでを自分の言葉で説明できる。

## 学習ログの完了条件

各テーマは、用語を覚えた時点ではなく、次の状態になったら完了とします。

- 小さな実験を自分で作れる
- 実行前に結果を予想できる
- デバッガや逆アセンブラで予想を検証できる
- 観察結果をCコードと対応させて説明できる
- 分からない点と次に試すことを記録できる

このロードマップは固定のカリキュラムではありません。実験で生まれた疑問に応じて、順序や内容を更新します。
